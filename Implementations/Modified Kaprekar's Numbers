#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() 
{
    long int p,q;
    cin>>p>>q;
    int flag=0;
    for(long int t=p;t<=q;t++)
        {
        if(t==0)
            cout<<t;
        else
        {
            long int n=1;
            for( long int i=10;i<=t;i=i*10)
                n=i;
            n=n*10;
            long int s=t*t;
            if((s%n)+(s/n)==t)
                {
                flag=1;
                cout<<t<<" ";
                }      
        }
    }
    
    if(flag==0)
        cout<<"INVALID RANGE";
    return 0;
}
